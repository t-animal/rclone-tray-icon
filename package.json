{
  "name": "rclone-tray-sync",
  "version": "0.0.1",
  "description": "Shows a tray icon for rclone bisync",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node index.ts",
    "lint": "npx prettier -w src/*ts src/**ts *js *json && npx eslint -f",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.7.5",
    "@typescript-eslint/parser": "^6.7.5",
    "eslint": "^8.51.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "^3.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
